substitutions:
  tesla_vin: ""
  ble_mac_address: "00:00:00:00:00:00"

esphome:
  on_boot:
    priority: 601.0
    then:
      - lambda: |-
          // ISO 3779 mandates VINs are exactly 17 alphanumeric characters.
          // sizeof() on a C string literal includes the null terminator, so a valid VIN yields sizeof == 18.
          // Any other value (empty, undefined substitution, wrong length) produces sizeof != 18 and fails here.
          static_assert(sizeof("$tesla_vin") == 18,
              "\n\n**********************************************************************************************"
              "\n***                                  IMPORTANT                                             ***"
              "\n***                                  =========                                             ***"
              "\n***                                                                                        ***"
              "\n*** Missing substitution in your yaml file for tesla_vin or is not a 17-character VIN.     ***"
              "\n*** You now need a substitution such as the following in your yaml:                        ***"
              "\n***   tesla_vin: !secret tesla_vin                                                         ***"
              "\n*** or if you have more than one cars (different secrets for each car):                    ***"
              "\n***   tesla_vin: !secret car1_tesla_vin                                                    ***"
              "\n***                                                                                        ***"
              "\n*** In secrets.yaml use:                                                                   ***"
              "\n***   tesla_vin: 5YJ3E1EA4JF000001 # for one car, for more than one car                    ***"
              "\n***   car1_tesla_vin:5YJ3E1EA4JF000001                                                     ***"
              "\n***                                                                                        ***"
              "\n*** See tesla-ble.example.yml for details.                                                 ***"
              "\n***                                                                                        ***"
              "\n**********************************************************************************************");
          // EUI-48 (IEEE 802) defines a 48-bit MAC address as 6 colon-separated hex octets (XX:XX:XX:XX:XX:XX):
          // 12 hex digits + 5 colons = 17 characters, so sizeof == 18 including the null terminator.
          static_assert("$ble_mac_address" != "00:00:00:00:00:00",
              "\n\n**********************************************************************************************"
              "\n***                                  IMPORTANT                                             ***"
              "\n***                                  =========                                             ***"
              "\n***                                                                                        ***"
              "\n*** Missing substitution in your yaml file for ble_mac_address or is not in                ***"
              "\n*** XX:XX:XX:XX:XX:XX format.                                                              ***"
              "\n*** You now need a substitution such as the following in your yaml:                        ***"
              "\n***   ble_mac_address: !secret ble_mac_address                                             ***"
              "\n*** or if you have more than one cars (different secrets for each car):                    ***"
              "\n***   ble_mac_address: !secret car1_ble_mac_address                                        ***"
              "\n***                                                                                        ***"
              "\n*** In secrets.yaml use:                                                                   ***"
              "\n***   ble_mac_address: AA:BB:CC:DD:EE:FF # for one car, for more than one car              ***"
              "\n***   car1_ble_mac_address: 5YJ3E1EA4JF000001                                              ***"
              "\n***                                                                                        ***"
              "\n*** See tesla-ble.example.yml for details.                                                 ***"
              "\n***                                                                                        ***"
              "\n**********************************************************************************************");

ble_client:
  - mac_address: $ble_mac_address
    id: ble_tesla_id

tesla_ble_vehicle:
  ble_client_id: ble_tesla_id
  id: tesla_ble_vehicle_id
  vin: $tesla_vin
  charger_switch: sw_charger
  defrost_switch: sw_defrost_car
  update_interval: 10s # default
  post_wake_poll_time: 300 # How long to poll for data after car awakes (s)
  poll_data_period: 60 # Normal period when polling for data when not asleep (s)
  poll_asleep_period: 60 # Period to poll for data when asleep (s)
  poll_charging_period: 10 # Period to poll for data when charging (s)
  ble_disconnected_min_time: 300 # Minimum time BLE must be disconnected before sensors are Unknwon (s)
  fast_poll_if_unlocked: 0 # if != 0, fast polls are enabled when unlocked
  wake_on_boot: 0 # != 0 wakes car on device boot

  is_asleep:
    id: "is_asleep"
    name: "Asleep"
  is_user_present:
    id: "is_user_present"
    name: "User presence"
  is_unlocked:
    id: "is_unlocked"
    name: "Doors"
  is_charge_flap_open:
    id: "is_charge_flap_open"
    name: "Charge flap"
  is_boot_open:
    id: "is_boot_open"
    name: "Boot"
  is_frunk_open:
    id: "is_frunk_open"
    name: "Frunk"
  shift_state:
    id: "shift_state"
    name: "Shift state"
  defrost_state:
    id: "defrost_state"
    name: "Defrost state"
  charge_state:
    id: "charge_state"
    name: "Charge level"
  odometer:
    id: "odometer"
    name: "Odometer"
    filters:
      - multiply: 0.01 # Odometer is in hundredths of a mile (can use this to convert to km)
  charge_current:
    id: "charge_current"
    name: "Charge current"
  charge_voltage:
    id: "charge_voltage"
    name: "Charge voltage"  
  charge_power:
    id: "charge_power"
    name: "Charge power"
  charge_energy_added:
    id: "charge_energy_added"
    name: "Charge energy added"
  charge_distance_added:
    id: "charge_distance_added"
    name: "Charge distance added"
  max_soc:
    id: "max_soc"
    name: "Charge limit"
  max_amps:
    id: "max_amps"
    name: "Current limit"
  mins_to_limit:
    id: "mins_to_limit"
    name: "Minutes to limit"
  battery_range:
    id: "battery_range"
    name: "Range"
  charging_state:
    id: "charging_state"
    name: "Charging state"
  charge_port_latch_state:
    id: "charge_port_latch_state"
    name: "Charge port latch state"
    disabled_by_default: true
  last_update:
    id: "last_update"
    name: "Last update"
  is_climate_on:
    id: "is_climate_on"
    name: "Climate"
  internal_temp:
    id: "internal_temp"
    name: "Interior"
  external_temp:
    id: "external_temp"
    name: "Exterior"
  windows_state:
    id: "windows_state"
    name: "Windows"
  tpms_pressure_fl:
    id: "tpms_pressure_fl"
    name: "Tyre pressure front left"
    disabled_by_default: true
  tpms_pressure_fr:
    id: "tpms_pressure_fr"
    name: "Tyre pressure front right"
    disabled_by_default: true
  tpms_pressure_rl:
    id: "tpms_pressure_rl"
    name: "Tyre pressure rear left"
    disabled_by_default: true
  tpms_pressure_rr:
    id: "tpms_pressure_rr"
    name: "Tyre pressure rear right"
    disabled_by_default: true
  ble_disconnected_time:
    id: "ble_disconnected_time"
    name: "BLE disconnected time"
    disabled_by_default: true
    entity_category: diagnostic
  charger_phases:
    id: "charger_phases"
    name: "Charger phases"
    disabled_by_default: true
    filters:
      - lambda: |-
          if (x == 2)
            return 3;
          return x;
  charge_rate:
    id: "charge_rate"
    name: "Charge rate"
    disabled_by_default: true
  driver_temp:
    id: "driver_temp"
    name: "Climate temperature" # although it's the driver side temperature setting, use it for the overall climate setting
    disabled_by_default: true
  command_latency:
    id: "command_latency"
    name: "Command latency"
    entity_category: diagnostic
  fast_charger_present:
    id: "fast_charger_present"
    name: "Fast charger"
  route_minutes_to_arrival:
    id: "route_minutes_to_arrival"
    name: "Route minutes to arrival"
  route_miles_to_arrival:
    id: "route_miles_to_arrival"
    name: "Route miles to arrival"
  speed_mph:
    id: "speed_mph"
    name: "Speed"
  power_kw:
    id: "power_kw"
    name: "Power"
  latitude:
    id: "latitude"
    name: "Latitude"
  longitude:
    id: "longitude"
    name: "Longitude"
  route_destination:
    id: "route_destination"
    name: "Destination"

button:
  - platform: template
    id: ble_pair
    name: Pair BLE key
    icon: mdi:key-wireless
    on_press:
      - lambda: id(tesla_ble_vehicle_id)->startPair();
    entity_category: diagnostic

  - platform: template
    id: btn_wake_up
    name: Wake up
    icon: mdi:sleep-off
    on_press:
    - lambda: id(tesla_ble_vehicle_id)->wakeVehicle();

  - platform: template
    id: btn_flash_light
    name: Flash light
    icon: mdi:car-light-high
    on_press:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage (tesla_ble_vehicle::BLE_CarServer_VehicleAction::FLASH_LIGHT, 0);

  - platform: template
    id: btn_sound_horn
    name: Sound horn
    icon: mdi:bugle
    on_press:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage (tesla_ble_vehicle::BLE_CarServer_VehicleAction::SOUND_HORN, 0);

  - platform: template
    id: btn_unlock_charge_port
    name: Unlock charge port
    icon: mdi:ev-plug-ccs2
    on_press:
      - lambda: id(tesla_ble_vehicle_id)->sendVCSECClosureMoveRequestMessage (VCSEC_ClosureMoveRequest_chargePort_tag, VCSEC_ClosureMoveType_E_CLOSURE_MOVE_TYPE_OPEN);

  - platform: template
    id: btn_unlatch_driver_door
    name: Unlatch driver door
    icon: mdi:car-door
    on_press:
      - lambda: id(tesla_ble_vehicle_id)->sendVCSECClosureMoveRequestMessage (VCSEC_ClosureMoveRequest_frontDriverDoor_tag, VCSEC_ClosureMoveType_E_CLOSURE_MOVE_TYPE_OPEN);
    disabled_by_default: true

  - platform: template
    id: btn_media_play
    name: Media play⁄pause # Normal / is not allowed here so use alternative fraction symbol
    icon: mdi:play-pause
    on_press:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage (tesla_ble_vehicle::BLE_CarServer_VehicleAction::MEDIA_PLAY, 0);
    disabled_by_default: true

  - platform: template
    id: btn_media_next_track
    name: Media next track
    icon: mdi:skip-next
    on_press:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage (tesla_ble_vehicle::BLE_CarServer_VehicleAction::MEDIA_NEXT_TRACK, 0);
    disabled_by_default: true

  - platform: template
    id: btn_media_previous_track
    name: Media previous track
    icon: mdi:skip-previous
    on_press:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage (tesla_ble_vehicle::BLE_CarServer_VehicleAction::MEDIA_PREVIOUS_TRACK, 0);
    disabled_by_default: true

  - platform: template
    id: btn_get_charge_schedule_state
    name: Get charge schedules
    icon: mdi:download-multiple
    on_press:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage (tesla_ble_vehicle::BLE_CarServer_VehicleAction::GET_CHARGE_SCHEDULE_STATE, 0);
    disabled_by_default: true

  - platform: template
    id: btn_regenerate_key
    name: Regenerate key
    icon: mdi:key-change
    on_press:
      - lambda: id(tesla_ble_vehicle_id)->regenerateKey();
    entity_category: diagnostic
    disabled_by_default: true

  - platform: template
    id: btn_force_data_update
    name: Force data update
    icon: mdi:database-sync
    on_press:
      - lambda: id(tesla_ble_vehicle_id)->enqueueVCSECInformationRequest(true);
    entity_category: diagnostic

sensor:
  - platform: ble_client
    type: rssi
    id: ble_signal
    ble_client_id: ble_tesla_id
    name: "BLE Signal"
    icon: mdi:bluetooth
    update_interval: 60s
    entity_category: diagnostic

switch:
  - platform: template
    id: sw_charger
    name: "Charger"
    optimistic: true
    icon: mdi:lightning-bolt
    lambda: |-
      if ((id(charging_state).state == "Charging") or (id(charging_state).state == "Starting") or (id(charging_state).state == "Calibrating")) {
        return true;
      } else {
        return false;
      }
    turn_on_action:
      - lambda: |-
          if (id(tesla_ble_vehicle_id).get_charging_state_raw() != CarServer_ChargeState_ChargingState_Complete_tag) { // Don't allow charging if last session has completed and no more configured
            id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_CHARGING_SWITCH, 1);
          } else {
            ESP_LOGI (esphome::tesla_ble_vehicle::TAG, "Charge request rejected as last session has completed. Reset to enable charging, eg raise the soc limit");
          }
    turn_off_action: 
      - lambda: |-
          id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_CHARGING_SWITCH, 0);

  - platform: template
    id: sw_sentry_mode
    name: "Sentry mode"
    optimistic: true
    icon: mdi:record-circle-outline
    assumed_state: true # we can't read the state 
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_SENTRY_SWITCH, 1);
    turn_off_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_SENTRY_SWITCH, 0);
  
  - platform: template
    id: sw_climate
    name: "Climate"
    optimistic: true
    icon: mdi:fan
    lambda: |-
      if (id(is_climate_on).state) {
        return true;
      } else {
        return false;
      }
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_HVAC_SWITCH, 1);
    turn_off_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_HVAC_SWITCH, 0);

  - platform: template
    id: sw_steering_wheel_heat
    name: "Steering wheel heat"
    optimistic: true
    icon: mdi:steering
    assumed_state: true # we can't read the state 
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_HVAC_STEERING_HEATER_SWITCH, 1);
    turn_off_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_HVAC_STEERING_HEATER_SWITCH, 0);
  
  - platform: template
    id: sw_low_power_mode
    name: "Low power mode"
    optimistic: true
    icon: mdi:power-sleep
    assumed_state: true # we can't read the state 
    restore_mode: RESTORE_DEFAULT_OFF
    disabled_by_default: true
    turn_on_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_LOW_POWER_MODE_SWITCH, 1);
    turn_off_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_LOW_POWER_MODE_SWITCH, 0);
  
  - platform: template
    id: sw_keep_accessory_power_on
    name: "Keep accessorry power on"
    optimistic: true
    icon: mdi:usb
    assumed_state: true # we can't read the state 
    restore_mode: RESTORE_DEFAULT_OFF
    disabled_by_default: true
    turn_on_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_KEEP_ACCESSORY_SWITCH, 1);
    turn_off_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_KEEP_ACCESSORY_SWITCH, 0);
  
  - platform: template
    id: sw_defrost_car
    name: Defrost car
    optimistic: true
    icon: mdi:snowflake-melt
    lambda: |-
      if (id(defrost_state).state != "Off") {
        return true;
      } else {
        return false;
      }
    turn_on_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage (tesla_ble_vehicle::BLE_CarServer_VehicleAction::DEFROST_CAR, 1);
    turn_off_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage (tesla_ble_vehicle::BLE_CarServer_VehicleAction::DEFROST_CAR, 0);

  - platform: template
    id: fast_poll_if_unlocked
    name: "Fast poll if unlocked"
    optimistic: true
    icon: mdi:car-door-lock-open
    entity_category: config
    restore_mode: RESTORE_DEFAULT_OFF
    disabled_by_default: true
    on_state:
      - if:
          condition:
            lambda: 'return x;'
          then:
            - lambda: id(tesla_ble_vehicle_id)->fast_poll_if_unlocked_ = 1;
          else:
            - lambda: id(tesla_ble_vehicle_id)->fast_poll_if_unlocked_ = 0;

  - platform: template
    id: wake_on_boot
    name: "Wake on boot"
    optimistic: true
    icon: mdi:sleep-off
    entity_category: config
    restore_mode: RESTORE_DEFAULT_OFF
    disabled_by_default: true
    on_state:
      - if:
          condition:
            lambda: 'return x;'
          then:
            - lambda: id(tesla_ble_vehicle_id)->wake_on_boot_ = 1;
          else:
            - lambda: id(tesla_ble_vehicle_id)->wake_on_boot_ = 0;

  - platform: ble_client
    id: sw_ble_connection
    ble_client_id: ble_tesla_id
    name: "BLE Connection"
    entity_category: diagnostic

lock:
  - platform: template
    id: lock_car
    name: "Lock car"
    optimistic: true
    icon: mdi:lock
    lambda: |-
      if (id(is_unlocked).state) {
          return LOCK_STATE_UNLOCKED;
        } else {
          return LOCK_STATE_LOCKED;
        }
    unlock_action:
      - lambda: |-
          id(tesla_ble_vehicle_id)->lockVehicle(VCSEC_RKEAction_E_RKE_ACTION_UNLOCK);
    lock_action:
      - lambda: |-
          id(tesla_ble_vehicle_id)->lockVehicle(VCSEC_RKEAction_E_RKE_ACTION_LOCK);

cover:
  - platform: template
    id: cov_vent
    name: "Vent"
    optimistic: false #### true
    icon: mdi:car-door
    device_class: door
    lambda: |-
      if (id(windows_state).state) {
        return COVER_OPEN;
      } else {
        return COVER_CLOSED;
      }
    close_action: #### This way round to make the arrows follow the desired window motion
      - lambda: |-
          id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage (tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_WINDOWS_SWITCH, 0);
    open_action:
      - lambda: |-
          id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage (tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_WINDOWS_SWITCH, 1);

  - platform: template
    id: cov_charge_port
    name: "Charge port"
    optimistic: true
    device_class: door
    icon: mdi:ev-plug-ccs2
    lambda: |-
      if (id(is_charge_flap_open).state) {
        return COVER_OPEN;
      } else {
        return COVER_CLOSED;
      }
#    restore_mode: RESTORE_DEFAULT_OFF
    open_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_OPEN_CHARGE_PORT_DOOR,1);
    close_action:
      - lambda: id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_CLOSE_CHARGE_PORT_DOOR,1);
  
  - platform: template
    id: cov_boot
    name: "Boot"
    optimistic: true
    device_class: door
    icon: mdi:car-back
    lambda: |-
      if (id(is_boot_open).state) {
        return COVER_OPEN;
      } else {
        return COVER_CLOSED;
      }
#    restore_mode: RESTORE_DEFAULT_OFF
    open_action:
      - lambda: id(tesla_ble_vehicle_id)->sendVCSECClosureMoveRequestMessage (VCSEC_ClosureMoveRequest_rearTrunk_tag, VCSEC_ClosureMoveType_E_CLOSURE_MOVE_TYPE_OPEN);
    close_action:
      - lambda: id(tesla_ble_vehicle_id)->sendVCSECClosureMoveRequestMessage (VCSEC_ClosureMoveRequest_rearTrunk_tag, VCSEC_ClosureMoveType_E_CLOSURE_MOVE_TYPE_CLOSE);

  - platform: template
    id: cov_frunk
    name: "Frunk"
    optimistic: true
    device_class: door
    icon: mdi:car
#    assumed_state: true # we can't read the state 
    lambda: |-
      if (id(is_frunk_open).state) {
        return COVER_OPEN;
      } else {
        return COVER_CLOSED;
      }
    open_action:
      - lambda: id(tesla_ble_vehicle_id)->sendVCSECClosureMoveRequestMessage (VCSEC_ClosureMoveRequest_frontTrunk_tag, VCSEC_ClosureMoveType_E_CLOSURE_MOVE_TYPE_OPEN);
    
number:
  - platform: template
    name: "Charging amps"
    id: charging_amps
    icon: mdi:ev-station
    unit_of_measurement: "A"
    mode: slider
    update_interval: 2s
    min_value: 0
    max_value: $charging_amps_max
    step: 1
    set_action:
      - lambda: |-
          int var_x = static_cast<int>(x);
          id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_CHARGING_AMPS, var_x);
    lambda: |-
      if (id(max_amps).has_state())
      {
        return id(max_amps).state;
      }
      else return (0);
  - platform: template
    name: "Charging limit"
    id: charging_limit
    icon: mdi:ev-station
    unit_of_measurement: "%"
    mode: slider
    update_interval: 2s
    min_value: 50
    max_value: 100
    step: 1
    set_action:
      - lambda: |-
          int var_x = static_cast<int>(x);
          id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_CHARGING_LIMIT, var_x);
    lambda: |-
      if (id(max_soc).has_state())
      {
        return id(max_soc).state;
      }
      else return (0);
  - platform: template
    name: "Climate temperature"
    id: set_climate_temp
    icon: mdi:thermometer-lines
    unit_of_measurement: "°C"
    mode: slider
    update_interval: 2s
    min_value: 15
    max_value: 28
    step: 0.5
    set_action:
      - lambda: |-
          id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::SET_CLIMATE_TEMP, x);
    lambda: |-
      if (id(driver_temp).has_state())
      {
        return id(driver_temp).state;
      }
      else return (0);
    disabled_by_default: true

  - platform: template
    id: post_wake_poll_time
    name: "Post wake poll time"
    icon: mdi:timelapse
    min_value: 0
    max_value: 1000
    mode: box
    step: 1
    restore_value: true
    initial_value: 300
    optimistic: true
    disabled_by_default: true
    entity_category: config
    on_value:
      - lambda: |-
          id(tesla_ble_vehicle_id)->post_wake_poll_time_ = x * 1000;
  - platform: template
    id: poll_data_period
    name: "Poll data period"
    icon: mdi:timer-outline
    min_value: 0
    max_value: 1000
    mode: box
    step: 1
    restore_value: true
    initial_value: 60
    optimistic: true
    disabled_by_default: true
    entity_category: config
    on_value:
      - lambda: |-
          id(tesla_ble_vehicle_id)->poll_data_period_ = x * 1000;
  - platform: template
    id: poll_asleep_period
    name: "Poll asleep period"
    icon: mdi:bed-clock
    min_value: 0
    max_value: 1000
    mode: box
    step: 1
    restore_value: true
    initial_value: 60
    optimistic: true
    disabled_by_default: true
    entity_category: config
    on_value:
      - lambda: |-
          id(tesla_ble_vehicle_id)->poll_asleep_period_ = x * 1000;
  - platform: template
    id: poll_charging_period
    name: "Poll charging period"
    icon: mdi:timer-sync-outline
    min_value: 0
    max_value: 1000
    mode: box
    step: 1
    restore_value: true
    initial_value: 10
    optimistic: true
    disabled_by_default: true
    entity_category: config
    on_value:
      - lambda: |-
          id(tesla_ble_vehicle_id)->poll_charging_period_ = x * 1000;
  - platform: template
    id: ble_disconnected_min_time
    name: "BLE disconnected min time"
    icon: mdi:bluetooth-connect
    min_value: 0
    max_value: 1000
    mode: box
    step: 1
    restore_value: true
    initial_value: 300
    optimistic: true
    disabled_by_default: true
    entity_category: config
    on_value:
      - lambda: |-
          id(tesla_ble_vehicle_id)->ble_disconnected_min_time_ = x * 1000;

text:
  - platform: template
    name: "Delete a charging schedule"
    id: del_charging_schedule
    icon: mdi:delete-clock
    mode: text
    update_interval: 2s
    set_action:
      - lambda: |-
          uint64_t id = strtoull(x.c_str(), nullptr, 10);
          if (id == 0)
          {
            ESP_LOGW("client.yml", "Invalid charging schedule ID %s, schedule delete not sent", x.c_str());
            return;
          }
          id(tesla_ble_vehicle_id)->sendCarServerVehicleActionMessage(tesla_ble_vehicle::BLE_CarServer_VehicleAction::DEL_CHARGING_SCHEDULE, 0, id);
    disabled_by_default: true
